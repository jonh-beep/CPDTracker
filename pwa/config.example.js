// Copy this file to config.local.js (which is gitignored) and paste in
// the deployed Apps Script web app URL. The shell uses this to know
// where "Open Tracker" should send you.
//
// Get the URL from: Apps Script editor → Deploy → Manage deployments → Web app URL
// Format: https://script.google.com/macros/s/<deploymentId>/exec

window.CPD_TRACKER_CONFIG = {
  webAppUrl: 'PASTE_DEPLOYMENT_URL_HERE'
};
